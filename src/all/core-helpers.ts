export {
  tap,
  attempt,
  match,
  not,
  and,
  or,
  isNullish,
  isString,
  isNumber,
  isBoolean,
  map,
  filter,
  flatMap,
  reduce,
  head,
  tail,
} from "../core/functional-utils"

export type { Matcher } from "../core/functional-utils"

export {
  hasOwn,
  keys,
  values,
  entries,
  fromEntries,
  mapValues,
  mapEntries,
  filterValues,
  filterKeys,
  pick,
  omit,
} from "../records/record-utils"
